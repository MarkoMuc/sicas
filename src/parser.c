#ifndef PARSER
#include <parser.h>
#endif

long builder(Vector *tokens, Vector *instr, Vector *sym, long index) {
  return index;
}

void parse_vector(Vector *vec) {
  Vector instructions;
  Vector sym;

  long vec_size = vec->count;
  for (long i = 0; i < vec_size; i++) {
  }
}
